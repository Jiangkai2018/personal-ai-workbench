# 复制为 .env 并填入真实值（.env 已被 .gitignore 忽略，不会提交）

# AI 初评（可选）：Anthropic 兼容接口，如智谱 BigModel https://open.bigmodel.cn/api/anthropic
# 配置后：想法一键转机会时自动 AI 初评；机会页可用「AI 预评 / AI 初评」
# 同时作为 Agent 板块的兜底厂商（Agent 优先读 data/config/ai-providers.json，可在后台管理）
WORKBENCH_AI_API_KEY=
WORKBENCH_AI_BASE_URL=https://open.bigmodel.cn/api/anthropic
WORKBENCH_AI_MODEL=glm-5.3

# Agent 板块：置 1 强制走确定性假模型（无外部依赖，用于演示 / e2e；正式使用勿开）
# WORKBENCH_AGENT_FAKE=0

# Agent 联网搜索（主路 bigmodel MCP）：不设则回退 WORKBENCH_AI_API_KEY
# WORKBENCH_SEARCH_API_KEY=
# WORKBENCH_SEARCH_MCP_URL=https://open.bigmodel.cn/api/mcp/web_search_prime/mcp
# 置 0 可整体关闭联网搜索
# WORKBENCH_SEARCH_MCP=0

# 备用 MCP（minimax，stdio/uvx）— 主路 bigmodel 不可用时自动切换；
# key 在 https://platform.minimaxi.com 订阅 Token Plan 后取得；留空则该链路自动跳过
WORKBENCH_SEARCH_MINIMAX_KEY=
WORKBENCH_SEARCH_MINIMAX_API_HOST=https://api.minimaxi.com
# 可选：自定义 stdio 启动命令（默认 uvx；PATH 没有 uv 时填绝对路径）
# WORKBENCH_SEARCH_MINIMAX_COMMAND=uvx

# 会话密钥（对外部署必改）
WORKBENCH_JWT_SECRET=

# 后端端口（默认 3000）
# PORT=5233

# 随手记账本特定配置（财务模块：成员归属/挂账账户，个人值放 .env）
WORKBENCH_SSJ_TOKEN=
WORKBENCH_SSJ_CLIENT_KEY=
WORKBENCH_SSJ_TRADING_ENTITY=
WORKBENCH_SSJ_ACCOUNT_ID=
WORKBENCH_SSJ_MEMBER_ME=
WORKBENCH_SSJ_MEMBER_FAMILY=
WORKBENCH_SSJ_BILL_OWNER_ME=
