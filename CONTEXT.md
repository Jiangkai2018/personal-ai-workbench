# 个人 AI 工作台

本地优先的个人成长闭环系统：想法 → 机会 → 目标 → 任务 → 执行 → 复盘 → 沉淀 → 反哺目标。数据是仓库里的 Markdown 文件，无数据库、无自助注册。

## Language

### Agent 板块

**Agent**：
运行在工作台里的多轮推理 AI 助手，能调用工具自主完成任务；用户通过会话与其交互。
_Avoid_: 聊天机器人、Copilot、Bot

**会话（Thread）**：
一次完整的多轮人机交互记录，是历史列表的单位。
_Avoid_: 对话记录（泛指时）、聊天室

**知识库**：
工作台内按目录分级组织的长期文档集合，供 Agent 检索以更准确回答，也接收用户的主动上传与 Agent 的产出沉淀；全家庭共享一棵树，不按个人/家庭范围隔离。
_Avoid_: 文档库、笔记库、RAG 库

**知识文件**：
知识库里的一篇 Markdown 文档，带标题等元信息，目录路径即其分类归属。

**技能（Skill）**：
预置给 Agent 的一套专项指令包，把某种任务（如政策风向分析）的做法固定下来；用户可显式触发，Agent 也可自主选用。
_Avoid_: 插件、命令、Prompt 模板

**产物**：
Agent 在会话中产出、尚不属于知识库的内容文档（以 Markdown 为主），以文档卡片形式嵌在消息流里，点击可在侧栏查看全貌。
_Avoid_: 附件、文件（未落盘前都不是知识库文件）

**落盘**：
把会话中产出的内容写成一篇知识文件、放进知识库合适位置的动作，由 Agent 执行、用户可纠正落点。
_Avoid_: 保存（太泛）、导入、归档
